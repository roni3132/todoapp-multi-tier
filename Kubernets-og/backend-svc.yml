apiVersion: v1
kind: Service
metadata:
    name: backend-svc
    labels:
        app: todoapp-backend
spec:
    type: LoadBalancer
    selector: 
        app: todoapp-backend
    ports:
        - protocol: TCP
          port: 80
          targetPort: 8080